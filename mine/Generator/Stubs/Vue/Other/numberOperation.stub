
  const numberOperation = (id, numberName, numberType = 'inc', numberValue = 1) => {
    {BUSINESS_EN_NAME}.numberOperation({ id, numberName, numberType, numberValue }).then( res => {
      res.success && ElMessage.success(res.message)
    }).catch( e => { console.log(e) } )
  }